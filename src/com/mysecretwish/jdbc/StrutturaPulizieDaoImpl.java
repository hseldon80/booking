/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package com.mysecretwish.jdbc;

import com.mysecretwish.dao.*;
import com.mysecretwish.factory.*;
import com.mysecretwish.dto.*;
import com.mysecretwish.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class StrutturaPulizieDaoImpl extends AbstractDAO implements StrutturaPulizieDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT spu_id, spu_lav_secco, spu_serv_stireria, spu_serv_lavand, spu_serv_giorn, spu_lustrascarpe, spu_stira_pant, struttura_str_id FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( spu_id, spu_lav_secco, spu_serv_stireria, spu_serv_lavand, spu_serv_giorn, spu_lustrascarpe, spu_stira_pant, struttura_str_id ) VALUES ( ?, ?, ?, ?, ?, ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET spu_id = ?, spu_lav_secco = ?, spu_serv_stireria = ?, spu_serv_lavand = ?, spu_serv_giorn = ?, spu_lustrascarpe = ?, spu_stira_pant = ?, struttura_str_id = ? WHERE spu_id = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE spu_id = ?";

	/** 
	 * Index of column spu_id
	 */
	protected static final int COLUMN_SPU_ID = 1;

	/** 
	 * Index of column spu_lav_secco
	 */
	protected static final int COLUMN_SPU_LAV_SECCO = 2;

	/** 
	 * Index of column spu_serv_stireria
	 */
	protected static final int COLUMN_SPU_SERV_STIRERIA = 3;

	/** 
	 * Index of column spu_serv_lavand
	 */
	protected static final int COLUMN_SPU_SERV_LAVAND = 4;

	/** 
	 * Index of column spu_serv_giorn
	 */
	protected static final int COLUMN_SPU_SERV_GIORN = 5;

	/** 
	 * Index of column spu_lustrascarpe
	 */
	protected static final int COLUMN_SPU_LUSTRASCARPE = 6;

	/** 
	 * Index of column spu_stira_pant
	 */
	protected static final int COLUMN_SPU_STIRA_PANT = 7;

	/** 
	 * Index of column struttura_str_id
	 */
	protected static final int COLUMN_STRUTTURA_STR_ID = 8;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 8;

	/** 
	 * Index of primary-key column spu_id
	 */
	protected static final int PK_COLUMN_SPU_ID = 1;

	/** 
	 * Inserts a new row in the struttura_pulizie table.
	 */
	public StrutturaPuliziePk insert(StrutturaPulizie dto) throws StrutturaPulizieDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			stmt = conn.prepareStatement( "SELECT nextval('seq_strutture')" );
			rs = stmt.executeQuery();
			if (rs.next()) {
				dto.setSpuId( rs.getLong( 1 ) );
				rs.close();
				stmt.close();
			}
		
			stmt = conn.prepareStatement( SQL_INSERT );
			int index = 1;
			stmt.setLong( index++, dto.getSpuId() );
			stmt.setString( index++, dto.getSpuLavSecco() );
			stmt.setString( index++, dto.getSpuServStireria() );
			stmt.setString( index++, dto.getSpuServLavand() );
			stmt.setString( index++, dto.getSpuServGiorn() );
			stmt.setString( index++, dto.getSpuLustrascarpe() );
			stmt.setString( index++, dto.getSpuStiraPant() );
			stmt.setLong( index++, dto.getStrutturaStrId() );
			System.out.println( "Executing " + SQL_INSERT + " with DTO: " + dto );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new StrutturaPulizieDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the struttura_pulizie table.
	 */
	public void update(StrutturaPuliziePk pk, StrutturaPulizie dto) throws StrutturaPulizieDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_UPDATE + " with DTO: " + dto );
			stmt = conn.prepareStatement( SQL_UPDATE );
			int index=1;
			stmt.setLong( index++, dto.getSpuId() );
			stmt.setString( index++, dto.getSpuLavSecco() );
			stmt.setString( index++, dto.getSpuServStireria() );
			stmt.setString( index++, dto.getSpuServLavand() );
			stmt.setString( index++, dto.getSpuServGiorn() );
			stmt.setString( index++, dto.getSpuLustrascarpe() );
			stmt.setString( index++, dto.getSpuStiraPant() );
			stmt.setLong( index++, dto.getStrutturaStrId() );
			stmt.setLong( 9, pk.getSpuId() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new StrutturaPulizieDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the struttura_pulizie table.
	 */
	public void delete(StrutturaPuliziePk pk) throws StrutturaPulizieDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_DELETE + " with PK: " + pk );
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setLong( 1, pk.getSpuId() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new StrutturaPulizieDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the struttura_pulizie table that matches the specified primary-key value.
	 */
	public StrutturaPulizie findByPrimaryKey(StrutturaPuliziePk pk) throws StrutturaPulizieDaoException
	{
		return findByPrimaryKey( pk.getSpuId() );
	}

	/** 
	 * Returns all rows from the struttura_pulizie table that match the criteria 'spu_id = :spuId'.
	 */
	public StrutturaPulizie findByPrimaryKey(long spuId) throws StrutturaPulizieDaoException
	{
		StrutturaPulizie ret[] = findByDynamicSelect( SQL_SELECT + " WHERE spu_id = ?", new Object[] {  new Long(spuId) } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the struttura_pulizie table that match the criteria ''.
	 */
	public StrutturaPulizie[] findAll() throws StrutturaPulizieDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY spu_id", null );
	}

	/** 
	 * Returns all rows from the struttura_pulizie table that match the criteria 'struttura_str_id = :strutturaStrId'.
	 */
	public StrutturaPulizie[] findByStruttura(long strutturaStrId) throws StrutturaPulizieDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE struttura_str_id = ?", new Object[] {  new Long(strutturaStrId) } );
	}

	/** 
	 * Returns all rows from the struttura_pulizie table that match the criteria 'spu_id = :spuId'.
	 */
	public StrutturaPulizie[] findWhereSpuIdEquals(long spuId) throws StrutturaPulizieDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE spu_id = ? ORDER BY spu_id", new Object[] {  new Long(spuId) } );
	}

	/** 
	 * Returns all rows from the struttura_pulizie table that match the criteria 'spu_lav_secco = :spuLavSecco'.
	 */
	public StrutturaPulizie[] findWhereSpuLavSeccoEquals(String spuLavSecco) throws StrutturaPulizieDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE spu_lav_secco = ? ORDER BY spu_lav_secco", new Object[] { spuLavSecco } );
	}

	/** 
	 * Returns all rows from the struttura_pulizie table that match the criteria 'spu_serv_stireria = :spuServStireria'.
	 */
	public StrutturaPulizie[] findWhereSpuServStireriaEquals(String spuServStireria) throws StrutturaPulizieDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE spu_serv_stireria = ? ORDER BY spu_serv_stireria", new Object[] { spuServStireria } );
	}

	/** 
	 * Returns all rows from the struttura_pulizie table that match the criteria 'spu_serv_lavand = :spuServLavand'.
	 */
	public StrutturaPulizie[] findWhereSpuServLavandEquals(String spuServLavand) throws StrutturaPulizieDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE spu_serv_lavand = ? ORDER BY spu_serv_lavand", new Object[] { spuServLavand } );
	}

	/** 
	 * Returns all rows from the struttura_pulizie table that match the criteria 'spu_serv_giorn = :spuServGiorn'.
	 */
	public StrutturaPulizie[] findWhereSpuServGiornEquals(String spuServGiorn) throws StrutturaPulizieDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE spu_serv_giorn = ? ORDER BY spu_serv_giorn", new Object[] { spuServGiorn } );
	}

	/** 
	 * Returns all rows from the struttura_pulizie table that match the criteria 'spu_lustrascarpe = :spuLustrascarpe'.
	 */
	public StrutturaPulizie[] findWhereSpuLustrascarpeEquals(String spuLustrascarpe) throws StrutturaPulizieDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE spu_lustrascarpe = ? ORDER BY spu_lustrascarpe", new Object[] { spuLustrascarpe } );
	}

	/** 
	 * Returns all rows from the struttura_pulizie table that match the criteria 'spu_stira_pant = :spuStiraPant'.
	 */
	public StrutturaPulizie[] findWhereSpuStiraPantEquals(String spuStiraPant) throws StrutturaPulizieDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE spu_stira_pant = ? ORDER BY spu_stira_pant", new Object[] { spuStiraPant } );
	}

	/** 
	 * Returns all rows from the struttura_pulizie table that match the criteria 'struttura_str_id = :strutturaStrId'.
	 */
	public StrutturaPulizie[] findWhereStrutturaStrIdEquals(long strutturaStrId) throws StrutturaPulizieDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE struttura_str_id = ? ORDER BY struttura_str_id", new Object[] {  new Long(strutturaStrId) } );
	}

	/**
	 * Method 'StrutturaPulizieDaoImpl'
	 * 
	 */
	public StrutturaPulizieDaoImpl()
	{
	}

	/**
	 * Method 'StrutturaPulizieDaoImpl'
	 * 
	 * @param userConn
	 */
	public StrutturaPulizieDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "struttura_pulizie";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected StrutturaPulizie fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			StrutturaPulizie dto = new StrutturaPulizie();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected StrutturaPulizie[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			StrutturaPulizie dto = new StrutturaPulizie();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		StrutturaPulizie ret[] = new StrutturaPulizie[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(StrutturaPulizie dto, ResultSet rs) throws SQLException
	{
		dto.setSpuId( rs.getLong( COLUMN_SPU_ID ) );
		dto.setSpuLavSecco( rs.getString( COLUMN_SPU_LAV_SECCO ) );
		dto.setSpuServStireria( rs.getString( COLUMN_SPU_SERV_STIRERIA ) );
		dto.setSpuServLavand( rs.getString( COLUMN_SPU_SERV_LAVAND ) );
		dto.setSpuServGiorn( rs.getString( COLUMN_SPU_SERV_GIORN ) );
		dto.setSpuLustrascarpe( rs.getString( COLUMN_SPU_LUSTRASCARPE ) );
		dto.setSpuStiraPant( rs.getString( COLUMN_SPU_STIRA_PANT ) );
		dto.setStrutturaStrId( rs.getLong( COLUMN_STRUTTURA_STR_ID ) );
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(StrutturaPulizie dto)
	{
	}

	/** 
	 * Returns all rows from the struttura_pulizie table that match the specified arbitrary SQL statement
	 */
	public StrutturaPulizie[] findByDynamicSelect(String sql, Object[] sqlParams) throws StrutturaPulizieDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new StrutturaPulizieDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the struttura_pulizie table that match the specified arbitrary SQL statement
	 */
	public StrutturaPulizie[] findByDynamicWhere(String sql, Object[] sqlParams) throws StrutturaPulizieDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new StrutturaPulizieDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
