/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package com.mysecretwish.jdbc;

import com.mysecretwish.dao.*;
import com.mysecretwish.factory.*;
import com.mysecretwish.dto.*;
import com.mysecretwish.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class PrenotazioneServiziDaoImpl extends AbstractDAO implements PrenotazioneServiziDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT prenotazione_servizi_id, descrizione_servizio, prenotazione_id FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( prenotazione_servizi_id, descrizione_servizio, prenotazione_id ) VALUES ( ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET prenotazione_servizi_id = ?, descrizione_servizio = ?, prenotazione_id = ? WHERE prenotazione_servizi_id = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE prenotazione_servizi_id = ?";

	/** 
	 * Index of column prenotazione_servizi_id
	 */
	protected static final int COLUMN_PRENOTAZIONE_SERVIZI_ID = 1;

	/** 
	 * Index of column descrizione_servizio
	 */
	protected static final int COLUMN_DESCRIZIONE_SERVIZIO = 2;

	/** 
	 * Index of column prenotazione_id
	 */
	protected static final int COLUMN_PRENOTAZIONE_ID = 3;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 3;

	/** 
	 * Index of primary-key column prenotazione_servizi_id
	 */
	protected static final int PK_COLUMN_PRENOTAZIONE_SERVIZI_ID = 1;

	/** 
	 * Inserts a new row in the prenotazione_servizi table.
	 */
	public PrenotazioneServiziPk insert(PrenotazioneServizi dto) throws PrenotazioneServiziDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			stmt = conn.prepareStatement( "SELECT nextval('seq_prenotazioni')" );
			rs = stmt.executeQuery();
			if (rs.next()) {
				dto.setPrenotazioneServiziId( rs.getLong( 1 ) );
				rs.close();
				stmt.close();
			}
		
			stmt = conn.prepareStatement( SQL_INSERT );
			int index = 1;
			stmt.setLong( index++, dto.getPrenotazioneServiziId() );
			stmt.setString( index++, dto.getDescrizioneServizio() );
			if (dto.isPrenotazioneIdNull()) {
				stmt.setNull( index++, java.sql.Types.INTEGER );
			} else {
				stmt.setLong( index++, dto.getPrenotazioneId() );
			}
		
			System.out.println( "Executing " + SQL_INSERT + " with DTO: " + dto );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new PrenotazioneServiziDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the prenotazione_servizi table.
	 */
	public void update(PrenotazioneServiziPk pk, PrenotazioneServizi dto) throws PrenotazioneServiziDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_UPDATE + " with DTO: " + dto );
			stmt = conn.prepareStatement( SQL_UPDATE );
			int index=1;
			stmt.setLong( index++, dto.getPrenotazioneServiziId() );
			stmt.setString( index++, dto.getDescrizioneServizio() );
			if (dto.isPrenotazioneIdNull()) {
				stmt.setNull( index++, java.sql.Types.INTEGER );
			} else {
				stmt.setLong( index++, dto.getPrenotazioneId() );
			}
		
			stmt.setLong( 4, pk.getPrenotazioneServiziId() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new PrenotazioneServiziDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the prenotazione_servizi table.
	 */
	public void delete(PrenotazioneServiziPk pk) throws PrenotazioneServiziDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_DELETE + " with PK: " + pk );
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setLong( 1, pk.getPrenotazioneServiziId() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new PrenotazioneServiziDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the prenotazione_servizi table that matches the specified primary-key value.
	 */
	public PrenotazioneServizi findByPrimaryKey(PrenotazioneServiziPk pk) throws PrenotazioneServiziDaoException
	{
		return findByPrimaryKey( pk.getPrenotazioneServiziId() );
	}

	/** 
	 * Returns all rows from the prenotazione_servizi table that match the criteria 'prenotazione_servizi_id = :prenotazioneServiziId'.
	 */
	public PrenotazioneServizi findByPrimaryKey(long prenotazioneServiziId) throws PrenotazioneServiziDaoException
	{
		PrenotazioneServizi ret[] = findByDynamicSelect( SQL_SELECT + " WHERE prenotazione_servizi_id = ?", new Object[] {  new Long(prenotazioneServiziId) } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the prenotazione_servizi table that match the criteria ''.
	 */
	public PrenotazioneServizi[] findAll() throws PrenotazioneServiziDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY prenotazione_servizi_id", null );
	}

	/** 
	 * Returns all rows from the prenotazione_servizi table that match the criteria 'prenotazione_id = :prenotazioneId'.
	 */
	public PrenotazioneServizi[] findByPrenotazioni(long prenotazioneId) throws PrenotazioneServiziDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE prenotazione_id = ?", new Object[] {  new Long(prenotazioneId) } );
	}

	/** 
	 * Returns all rows from the prenotazione_servizi table that match the criteria 'prenotazione_servizi_id = :prenotazioneServiziId'.
	 */
	public PrenotazioneServizi[] findWherePrenotazioneServiziIdEquals(long prenotazioneServiziId) throws PrenotazioneServiziDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE prenotazione_servizi_id = ? ORDER BY prenotazione_servizi_id", new Object[] {  new Long(prenotazioneServiziId) } );
	}

	/** 
	 * Returns all rows from the prenotazione_servizi table that match the criteria 'descrizione_servizio = :descrizioneServizio'.
	 */
	public PrenotazioneServizi[] findWhereDescrizioneServizioEquals(String descrizioneServizio) throws PrenotazioneServiziDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE descrizione_servizio = ? ORDER BY descrizione_servizio", new Object[] { descrizioneServizio } );
	}

	/** 
	 * Returns all rows from the prenotazione_servizi table that match the criteria 'prenotazione_id = :prenotazioneId'.
	 */
	public PrenotazioneServizi[] findWherePrenotazioneIdEquals(long prenotazioneId) throws PrenotazioneServiziDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE prenotazione_id = ? ORDER BY prenotazione_id", new Object[] {  new Long(prenotazioneId) } );
	}

	/**
	 * Method 'PrenotazioneServiziDaoImpl'
	 * 
	 */
	public PrenotazioneServiziDaoImpl()
	{
	}

	/**
	 * Method 'PrenotazioneServiziDaoImpl'
	 * 
	 * @param userConn
	 */
	public PrenotazioneServiziDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "prenotazione_servizi";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected PrenotazioneServizi fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			PrenotazioneServizi dto = new PrenotazioneServizi();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected PrenotazioneServizi[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			PrenotazioneServizi dto = new PrenotazioneServizi();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		PrenotazioneServizi ret[] = new PrenotazioneServizi[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(PrenotazioneServizi dto, ResultSet rs) throws SQLException
	{
		dto.setPrenotazioneServiziId( rs.getLong( COLUMN_PRENOTAZIONE_SERVIZI_ID ) );
		dto.setDescrizioneServizio( rs.getString( COLUMN_DESCRIZIONE_SERVIZIO ) );
		dto.setPrenotazioneId( rs.getLong( COLUMN_PRENOTAZIONE_ID ) );
		if (rs.wasNull()) {
			dto.setPrenotazioneIdNull( true );
		}
		
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(PrenotazioneServizi dto)
	{
	}

	/** 
	 * Returns all rows from the prenotazione_servizi table that match the specified arbitrary SQL statement
	 */
	public PrenotazioneServizi[] findByDynamicSelect(String sql, Object[] sqlParams) throws PrenotazioneServiziDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new PrenotazioneServiziDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the prenotazione_servizi table that match the specified arbitrary SQL statement
	 */
	public PrenotazioneServizi[] findByDynamicWhere(String sql, Object[] sqlParams) throws PrenotazioneServiziDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new PrenotazioneServiziDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
